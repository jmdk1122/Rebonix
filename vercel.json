{
  "routes": [
    // 1. 根路径 ( / ) 的处理：将根路径请求导向您的主文件
    {
      "src": "/",
      "dest": "/Rebonix_whitepaper_full_v1.1.html" 
    },
    
    // 2. 所有带 .html 扩展名的文件：确保所有 HTML 文件都能通过其文件名访问
    {
      "src": "/(.*\\.html)", 
      "dest": "/$1"
    },
    
    // 3. 通用静态资源 (CSS, JS, 图片等)：确保其他静态文件也能被访问
    {
      "src": "/(.*)", 
      "dest": "/$1"
    }
  ]
}